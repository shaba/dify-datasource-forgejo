[project]
name = "dify-datasource-forgejo"
version = "0.0.1"
description = "Ingest Forgejo and Gitea repositories, issues, pull requests and READMEs into a Knowledge base"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "requests>=2.32",
]

[dependency-groups]
dev = []
